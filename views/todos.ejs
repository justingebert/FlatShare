
<br>
<br>
<div>
    <div>Important</div> 
    <% todos.forEach(todo=> { %>
        <h5 class=" px-3 border-2 border-black "><%= todo.name %>
        <% let checked = todo.completed ? "checked" : "" %>
        <input type="checkbox" class="px-3 border-2"<%= checked %> />
    <% }); %>
</div>

<div>
    <form action="/todos" method="post">
        input name: <input type="text" name="name" />
        completed?<input type="checkbox" name="completed" value="false" />
        <input type="submit" name="submit">
    </form>
</div>